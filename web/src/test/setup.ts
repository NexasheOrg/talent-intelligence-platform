/**
 * Runs before every test file (wired up in vite.config.ts).
 *
 * `@testing-library/jest-dom` adds the readable matchers - `toBeInTheDocument()`,
 * `toHaveTextContent()` - so a failing assertion tells you what was on screen.
 */

import '@testing-library/jest-dom/vitest'
