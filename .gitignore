# data outputs (never commit data, even synthetic). Small test fixtures under
# tests/ or a fixtures/ dir are allowed on purpose.
data/seed/out/
data/raw/
*.parquet
**/*.data.csv

# the local SQLite warehouse built by the no-Docker route - regenerate it, don't commit it
data/local/
*.db

# trained models and their metrics are build outputs
ml/models/

# python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# node
node_modules/
dist/
build/
.vite/

# java
target/
*.class

# env & secrets
.env
.env.*
*.pem
*.key

# infra state
*.tfstate
*.tfstate.*
.terraform/

# os / editor
.DS_Store
.idea/
.vscode/
